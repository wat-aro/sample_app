<h1><%= t('application') %></h1>

<p><%= @user.name %> <%= t('email.hi') %></p>

<p>
  <%= t('email.welcome') %>
</p>

<%= link_to t('email.activate'), confirmation_url(@resource, :confirmation_token => @token) %>
